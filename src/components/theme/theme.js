import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    common: {
      white: "#FFFFFF",
      background: "#F6F9FC",
      lightGrey: "#EFEFEF",
      darkGrey: "#82889A",
      primaryColor: "#C24B5A",
    },
  },
});

export default theme;
